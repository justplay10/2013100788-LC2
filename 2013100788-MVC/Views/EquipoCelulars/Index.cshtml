@model IEnumerable<_2013100788_ENT.Entities.EquipoCelular>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Evaluacions.desc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.descrip)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Evaluacions.desc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.descrip)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EquipCelId }) |
            @Html.ActionLink("Details", "Details", new { id=item.EquipCelId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EquipCelId })
        </td>
    </tr>
}

</table>

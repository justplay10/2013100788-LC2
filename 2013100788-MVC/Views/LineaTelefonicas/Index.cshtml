@model IEnumerable<_2013100788_ENT.Entities.LineaTelefonica>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.desc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TipoLinea)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.desc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TipoLinea)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LinTelfId }) |
            @Html.ActionLink("Details", "Details", new { id=item.LinTelfId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LinTelfId })
        </td>
    </tr>
}

</table>

@model IEnumerable<_2013100788_ENT.Entities.CentroAtencion>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Direccion.desc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.desc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Direccion.desc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.desc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CenAteId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CenAteId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CenAteId })
        </td>
    </tr>
}

</table>
